// =============================================
// CONFIG.JS — Configurazione PRODUZIONE
// =============================================
if (!window.CONFIG) {
  window.CONFIG = {
    // URL del backend in produzione
    API_URL: 'https://api.ironzone.it/api',
    API_TIMEOUT: 5000,
    DEBUG: false
  };
}
